---
cycle_count: 2
max_cycles: 3
status: "resolved"
severity: P3
---
# Rework ticket — agent-quality-improvement

## 1. Failure Reason

**FALSE RESOLVED — Cycle 1 fix was never applied.** The rework_ticket.md was marked `status: "resolved"` but `git diff HEAD` confirms **zero changes** to the target files. Both `skills/forge-orchestrator/SKILL.md` and `ide/cursor/agents/forge-orchestrator.md` are in the exact same state as the original implementation.

**Spec deviation (FR-008, NFR-002) — still present:**
1. No `<!-- sync: ide/shared/workflow-orchestrator-parity.md -->` comments on the CKP table
2. Memory Curation Protocol still fully duplicated inline (STEP 1/2/3) instead of referencing shared parity

**Root cause:** The Cursor orchestrator is compiled from `skills/forge-orchestrator/SKILL.md` by `compile-agents-from-skills.py`. The fix must be applied to the SKILL.md source, then recompiled via `bash ide/install.sh`.

**CKP-3 status: ⚠️ Cycle 2/3.** This is the 2nd rework. One cycle remains before the emergency brake triggers.

## 2. Affected Files

- `skills/forge-orchestrator/SKILL.md` — **source-of-truth** (169 lines, unchanged)
- `ide/cursor/agents/forge-orchestrator.md` — **compiled agent** (will be regenerated by installer after SKILL.md fix)

**Reference files (read-only, already correct):**
- `ide/vscode/agents/forge-orchestrator.agent.md` — lines ~40, ~104: example of correct sync comment + reference pattern
- `ide/opencode/agents/flowforge.md` — lines ~37, ~46: example of correct sync comment + reference pattern
- `ide/shared/workflow-orchestrator-parity.md` — lines 83-120: canonical Memory Curation Protocol

## 3. Correction Instruction

### Step 1: Fix `skills/forge-orchestrator/SKILL.md` (source-of-truth)

**Change 1A — Add sync comment to CKP table:**

**Location:** Before line 17 (`| CKP | Phase | Color | Type | If skipped |`).

**Add this line before the table:**
```
<!-- sync: ide/shared/workflow-orchestrator-parity.md -->
```

**Change 1B — Replace inline Memory Curation Protocol with reference:**

**Remove:** Lines 91-119 (the full inline Memory Curation Protocol — everything from `## Memory Curation Protocol` through the end of the STEP 3 code block on line 119).

**Replace with:**
```markdown
<!-- sync: ide/shared/workflow-orchestrator-parity.md -->
## Memory Curation Protocol

After receiving handoff from `forge-arch` or `forge-dev`, read the `## Memory Signal` block and apply the Memory Curation Protocol. See `ide/shared/workflow-orchestrator-parity.md` for the canonical 3-step process (STEP 1: eligible type? → STEP 2: was there friction? → STEP 3: already in Engram?). All other agents (forge-plan, forge-verify, forge-discovery) do not emit Memory Signal — skip curation for them.
```

**What to preserve in SKILL.md:**
- CKP metrics block (lines 121-128) — keep as-is (CKP metrics are orchestrator-specific)
- Session close mandatory note (lines 130-134) — keep as-is
- Rework intake section (lines 136-145) — keep as-is
- Close without PM-* (lines 149-151) — keep as-is
- Golden rules (lines 153-159) — keep as-is
- Delegation protocol (lines 161-165) — keep as-is
- Configuration section (lines 167-169) — keep as-is

### Step 2: Recompile Cursor agents

```bash
bash ide/install.sh
```

This runs `compile-agents-from-skills.py` which regenerates `ide/cursor/agents/forge-orchestrator.md` from the updated `skills/forge-orchestrator/SKILL.md`.

### Step 3: Verify both files

Run these commands and confirm:
```bash
# SKILL.md checks
rg '<!-- sync:' skills/forge-orchestrator/SKILL.md
# Expected: 2 matches (CKP table + Memory Curation Protocol)

rg 'STEP 1 — Eligible type' skills/forge-orchestrator/SKILL.md
# Expected: ZERO matches

# Cursor agent checks
rg '<!-- sync:' ide/cursor/agents/forge-orchestrator.md
# Expected: 2 matches

rg 'STEP 1 — Eligible type' ide/cursor/agents/forge-orchestrator.md
# Expected: ZERO matches
```

## 4. Close Criteria

- [ ] `skills/forge-orchestrator/SKILL.md` has `<!-- sync: ide/shared/workflow-orchestrator-parity.md -->` before CKP table
- [ ] `skills/forge-orchestrator/SKILL.md` inline Memory Curation Protocol replaced with reference paragraph
- [ ] `bash ide/install.sh` recompiles successfully (exit code 0)
- [ ] `rg '<!-- sync:' skills/forge-orchestrator/SKILL.md` returns 2 matches
- [ ] `rg '<!-- sync:' ide/cursor/agents/forge-orchestrator.md` returns 2 matches
- [ ] `rg 'STEP 1 — Eligible type' skills/forge-orchestrator/SKILL.md` returns ZERO matches
- [ ] `rg 'STEP 1 — Eligible type' ide/cursor/agents/forge-orchestrator.md` returns ZERO matches
- [ ] Plan.md Task 3.2 acceptance criteria for Cursor orchestrator now truly met
