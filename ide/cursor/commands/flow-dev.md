Fase Dev (implementación).

```
/flow-dev
```

Requiere `plan.md` aprobado. El orquestador delega a `forge-dev`. Si hay `rework_ticket.md` abierto, dev lo prioriza. Debe marcar checklist en `plan.md`.
