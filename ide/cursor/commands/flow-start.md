Iniciar feature FlowForge (Discovery → Spec).

```
/flow-start <descripcion de la feature>
```

El orquestador delega a `forge-discovery` → `forge-arch`. Artefactos: `.ai-work/{feature-slug}/spec.md`. CKP-1: aprobación humana antes de `/flow-plan`.
