Rework / bug report (orquestador no arregla inline).

```
/flow-rework
```

O describí el fallo ("reporté un error", "falló la prueba"). El orquestador crea/actualiza `rework_ticket.md` y delega a `forge-dev`.
